/**
 * Settings Header Component
 * Note: The title and description are now shown in the AdminLayout header bar.
 * This component is kept for backwards compatibility but returns nothing.
 */

export function SettingsHeader(): JSX.Element {
  // Component is now empty as title and description moved to AdminLayout header
  // Keeping the component for backwards compatibility
  return <></>;
}
